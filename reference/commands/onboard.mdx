---
title: "/opsx:onboard"
description: "Guided interactive tutorial through the complete OpenSpec workflow using your actual codebase."
---

`/opsx:onboard` is an interactive tutorial that walks you through a complete OpenSpec workflow cycle with narration. It uses your actual codebase — finding a real improvement opportunity, creating a change, implementing it, and archiving it — so you learn by doing.

## Syntax

```text
/opsx:onboard
```

## Arguments

This command takes no arguments.

## What it does

- Walks through a complete workflow cycle with step-by-step narration
- Scans your codebase for real improvement opportunities
- Creates an actual change with real artifacts
- Implements actual work (small, safe changes)
- Archives the completed change
- Explains each step as it happens

## Phases

| Phase | What happens |
|---|---|
| 1 | Welcome and codebase analysis |
| 2 | Finding an improvement opportunity |
| 3 | Creating a change (`/opsx:new`) |
| 4 | Writing the proposal |
| 5 | Creating specs |
| 6 | Writing the design |
| 7 | Creating tasks |
| 8 | Implementing tasks (`/opsx:apply`) |
| 9 | Verifying implementation |
| 10 | Archiving the change |
| 11 | Summary and next steps |

## Example

```text
You: /opsx:onboard

AI:  Welcome to OpenSpec!

     I'll walk you through the complete workflow using your actual codebase.
     We'll find something small to improve, create a proper change for it,
     implement it, and archive it.

     Let me scan your codebase for opportunities...

     [Analyzes codebase]

     I found a few things we could work on:
     1. Add input validation to the contact form
     2. Improve error messages in the auth flow
     3. Add loading states to async buttons

     Which interests you? (or suggest something else)
```

## Tips

- Best for new users learning the OpenSpec workflow for the first time
- Uses real code from your project, not toy examples
- Creates a real change you can keep or discard after the tutorial
- Plan for 15–30 minutes to complete the full onboarding
- The change created during onboarding follows the same artifact structure as any other change

## Related

<CardGroup cols={2}>
  <Card title="Slash commands overview" icon="terminal" href="/reference/commands/overview">
    See all available slash commands.
  </Card>
  <Card title="/opsx:propose" icon="bolt" href="/reference/commands/propose">
    The fastest path to creating a change (core profile).
  </Card>
  <Card title="/opsx:new" icon="plus" href="/reference/commands/new">
    Start a new change scaffold (expanded workflow).
  </Card>
  <Card title="Workflows" icon="diagram-project" href="/workflows/overview">
    Learn about workflow patterns and when to use each command.
  </Card>
</CardGroup>
