---
title: "/opsx:explore"
description: "Think through ideas, investigate problems, and clarify requirements before committing to a change."
---

`/opsx:explore` opens an exploratory conversation with no structure required. Use it when requirements are unclear, you need to investigate the codebase, or you want to compare approaches before deciding what to build.

## Syntax

```text
/opsx:explore [topic]
```

## Arguments

<ParamField path="topic" type="string">
  What you want to explore or investigate. If not provided, the AI will ask what you'd like to explore.
</ParamField>

## What it does

- Opens an exploratory conversation with no structure required
- Investigates the codebase to answer questions
- Compares options and approaches
- Creates visual diagrams to clarify thinking
- Can transition to `/opsx:propose` (core profile) or `/opsx:new` (expanded workflow) when insights crystallize

## Example

```text
You: /opsx:explore

AI:  What would you like to explore?

You: How should we handle authentication for the mobile app?

AI:  Let me investigate your current auth setup...
     [Analyzes codebase]

     I see you're using session-based auth for the web app.
     For mobile, we have three main options:

     1. JWT tokens (stateless, works offline)
     2. OAuth2 (if you need third-party login)
     3. Extend current sessions (simpler, but requires connectivity)

     Your API already has CORS configured. Which direction interests you?

You: Let's go with JWT. Can we start a change for that?

AI:  Ready when you are. Run /opsx:propose add-jwt-auth to begin.
```

## Tips

- Use when requirements are unclear or you need to investigate the codebase first
- No artifacts are created during exploration — it is purely a thinking and investigation phase
- Good for comparing multiple approaches before deciding on one
- The AI can read files and search the codebase during exploration
- When you're ready to proceed, the AI will suggest the right command to start your change

## Related

<CardGroup cols={2}>
  <Card title="/opsx:propose" icon="bolt" href="/reference/commands/propose">
    Create a change once you've decided on an approach.
  </Card>
  <Card title="/opsx:new" icon="plus" href="/reference/commands/new">
    Start a change scaffold (expanded workflow).
  </Card>
</CardGroup>
